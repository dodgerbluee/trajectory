/**
 * Deprecated duplicate of `features/visits/visit-form/VisitFormSidebar`.
 * Kept only to avoid breaking stale imports; do not use.
 */

export type VisitFormSidebarProps = never;

export function VisitFormSidebar(): never {
  throw new Error('Deprecated: import VisitFormSidebar from @visit-form (features/visits/visit-form) instead of src/visit-form.');
}

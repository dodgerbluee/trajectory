/**
 * Jest setup file. Global setup can be added here if needed.
 * Supertest is required directly in tests that need it (e.g. authorization.test.ts)
 * so the suite can run even when setup runs before all modules are resolved.
 */

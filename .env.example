# Database Configuration
DB_PASSWORD=development_password
POSTGRES_DB=trajectory
POSTGRES_USER=trajectory_user

# Optional: Log level (error | warn | info | debug). Default: info.
# LOG_LEVEL=info

# Optional: Timezone for app and DB (e.g. America/New_York, Europe/London). Default: UTC.
# TZ=UTC

# Authentication Configuration
# Generate strong random secrets for production (e.g., using: openssl rand -base64 32)
JWT_SECRET=change-me-in-production-use-strong-random-secret
JWT_REFRESH_SECRET=change-me-in-production-use-strong-random-refresh-secret

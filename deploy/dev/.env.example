# =============================================================================
# Trajectory – Environment variables (development / reference)
# =============================================================================
# Copy to .env and set values. Do not use these placeholders in production.
# For production, see .env.prod.example and docs/DEPLOYMENT.md.
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# Required for backend. With Docker Compose, build from POSTGRES_* and DB_PASSWORD:
#   postgresql://POSTGRES_USER:DB_PASSWORD@localhost:5018/POSTGRES_DB
# DATABASE_URL=postgresql://trajectory_user:development_password@localhost:5018/trajectory

DB_PASSWORD=development_password
DB_PORT=5432
POSTGRES_DB=trajectory
POSTGRES_USER=trajectory_user

# -----------------------------------------------------------------------------
# Authentication (required for login/sessions)
# -----------------------------------------------------------------------------
# Generate strong random secrets for production, e.g.: openssl rand -base64 32
# In production these MUST be set and must not be the default placeholders.
JWT_SECRET=change-me-in-production-use-strong-random-secret
JWT_REFRESH_SECRET=change-me-in-production-use-strong-random-refresh-secret

# -----------------------------------------------------------------------------
# Backend server
# -----------------------------------------------------------------------------
# APP_PORT=5017
# HOST=0.0.0.0

# -----------------------------------------------------------------------------
# Backend – optional (defaults shown)
# -----------------------------------------------------------------------------
# Log level: error | warn | info | debug. Default: info
# LOG_LEVEL=info

# Timezone for app and DB (e.g. America/New_York, Europe/London). Default: UTC
# TZ=UTC

# When set, backend serves the built frontend from this directory (e.g. "public")
# FRONTEND_DIR=public

# File storage paths (defaults are container paths; override for custom layout)
# UPLOAD_DIR=/app/uploads
# AVATAR_DIR=/app/avatars

# -----------------------------------------------------------------------------
# Frontend (Vite) – optional; used when running frontend dev server or custom proxy
# -----------------------------------------------------------------------------
# API base URL. Empty or relative when UI is served by same app (production build)
# VITE_API_URL=

# Base path for the app (e.g. "/app" for https://example.com/app). Default: /
# VITE_BASE_PATH=

# Override version shown in UI (defaults to package.json version)
# VITE_APP_VERSION=

# =============================================================================
# Trajectory – Production environment variables
# =============================================================================
# Copy to .env and set appropriate values. Never use placeholder secrets.
# See docs/DEPLOYMENT.md for full documentation.
# =============================================================================

# Docker image (override for a specific version)
# IMAGE=ghcr.io/your-org/trajectory:latest
# IMAGE=ghcr.io/your-org/trajectory:dev-20250125-abc1234

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
DB_PORT=5432
POSTGRES_DB=trajectory
POSTGRES_USER=trajectory_user

# Authentication – required for login/sessions. Generate strong random secrets, e.g.:
#   openssl rand -base64 32
# The app will not start in production if these are missing or still placeholders.
JWT_SECRET=CHANGE_THIS_USE_STRONG_RANDOM_SECRET
JWT_REFRESH_SECRET=CHANGE_THIS_USE_STRONG_RANDOM_REFRESH_SECRET

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# APP_PORT=5017
# LOG_LEVEL=info
# TZ=UTC
# GITHUB_REPOSITORY=your-org/trajectory

# File storage: backend defaults to /app/uploads and /app/avatars inside the
# container. Override only if you mount volumes to different paths.
# UPLOAD_DIR=/app/uploads
# AVATAR_DIR=/app/avatars
